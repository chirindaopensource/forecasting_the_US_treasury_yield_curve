# =============================================================================
# requirements.txt
# =============================================================================
# Production-grade dependency specification for:
# "Forecasting the U.S. Treasury Yield Curve: A Distributionally Robust 
# Machine Learning Approach"
#
# Reference: Liu, J., & Cheng, M.-Y. (2026). arXiv:2601.04608
# Author: CS Chirinda
# Version: 1.0.0
# Python: >=3.9,<3.13
# =============================================================================

# -----------------------------------------------------------------------------
# Core Numerical Computing Stack
# -----------------------------------------------------------------------------
# NumPy: Vectorized operations, linear algebra, random number generation
# Required for: Matrix operations, eigen-decomposition, SVD, rolling windows
numpy>=1.24.0,<2.0.0

# Pandas: Time series data structures, rolling windows, date arithmetic
# Required for: DataFrame operations, DateOffset, MonthEnd, temporal indexing
pandas>=2.0.0,<3.0.0

# SciPy: Optimization solvers, statistical functions, linear algebra
# Required for: linprog (LP solver), minimize (NLS), rankdata, pseudoinverse
scipy>=1.10.0,<2.0.0

# -----------------------------------------------------------------------------
# Econometric & Time Series Analysis
# -----------------------------------------------------------------------------
# Statsmodels: Econometric modeling, unit root tests, diagnostic tests
# Required for: adfuller (ADF test), breaks_cusumolsresid (CUSUM), VAR, OLS
statsmodels>=0.14.0,<1.0.0

# -----------------------------------------------------------------------------
# Machine Learning & Model Selection
# -----------------------------------------------------------------------------
# Scikit-learn: Random Forests, cross-validation, preprocessing
# Required for: RandomForestRegressor, RandomizedSearchCV, TimeSeriesSplit
scikit-learn>=1.3.0,<2.0.0

# SHAP: Model interpretation via Shapley values
# Required for: TreeExplainer, force_plot, summary_plot for XAI
shap>=0.44.0,<1.0.0

# -----------------------------------------------------------------------------
# Structural Break Detection & Change Point Analysis
# -----------------------------------------------------------------------------
# Ruptures: Change point detection algorithms
# Required for: PELT algorithm with RBF cost functions, regime shift detection
ruptures>=1.1.9,<2.0.0

# -----------------------------------------------------------------------------
# Optional: Enhanced Optimization & Numerical Stability
# -----------------------------------------------------------------------------
# CVXPY: Convex optimization framework (optional, for QP formulations)
# Uncomment if using explicit convex programming for portfolio constraints
# cvxpy>=1.4.0,<2.0.0

# -----------------------------------------------------------------------------
# Development & Quality Assurance
# -----------------------------------------------------------------------------
# Type checking (development only - not runtime dependency)
# mypy>=1.7.0

# Code formatting (development only)
# black>=23.0.0
# isort>=5.12.0

# Testing framework (development only)
# pytest>=7.4.0
# pytest-cov>=4.1.0

# -----------------------------------------------------------------------------
# Compatibility Notes
# -----------------------------------------------------------------------------
# 1. NumPy 2.0+ introduces breaking changes; pin to <2.0.0 for stability
# 2. Pandas 2.0+ requires numpy>=1.20.3; compatibility verified
# 3. Statsmodels 0.14+ resolves deprecation warnings with pandas 2.0+
# 4. SHAP 0.44+ includes performance improvements for TreeExplainer
# 5. Scikit-learn 1.3+ maintains API stability for RandomForest
# 6. Python 3.9+ required for type hints (PEP 585) and dict merge operators
#
# Installation Command:
# pip install -r requirements.txt
#
# For reproducible environments, generate locked dependencies:
# pip freeze > requirements-lock.txt
# =============================================================================